<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/navigation_more"
        android:icon="@mipmap/ic_launcher"
        android:title=""
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/navigation_scan"
                android:icon="@mipmap/ic_launcher"
                android:title="@string/menu_item_scan"
                android:visible="false"
                app:showAsAction="never"/>
            <item
                android:id="@+id/navigation_invite"
                android:icon="@mipmap/ic_launcher"
                android:title="@string/menu_item_invite"
                android:visible="false"
                app:showAsAction="never"/>
            <item
                android:id="@+id/navigation_join"
                android:icon="@mipmap/ic_launcher"
                android:title="@string/menu_item_join"
                android:visible="false"
                app:showAsAction="never"/>
            <item
                android:id="@+id/navigation_create"
                android:icon="@mipmap/ic_launcher"
                android:visible="false"
                android:title="@string/menu_item_create"
                app:showAsAction="never">
            </item>
        </menu>
    </item>

</menu>